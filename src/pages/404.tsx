import * as React from "react";
import { Link } from "gatsby";

// markup
const NotFoundPage = () => {
    return (
        <>
            <h1 style={{ textAlign: "center", color: "white" }}>404</h1>
            <h3 style={{ textAlign: "center", color: "white", marginTop: 30 }}>
                Page not found.
            </h3>
        </>
    );
};

export default NotFoundPage;
